{
  "scripts": {
    "start": "concurrently \"npm start --prefix client\" \"npm run server --prefix server\"",
    "server": "nodemon index.js --prefix server",
    "client": "npm start --prefix client",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^3.1.7"
  }
}
