import React from "react"

const styles = {
  logo: {
    fontWeight: '700',
    fontSize: '2rem',
  }
}

const Logo = () => {
  return (
    <div style={styles.logo}>
      Menú
    </div>
  )
}

export default Logo
